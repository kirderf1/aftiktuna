{
  "type": "object",
  "properties": {
    "default_stats": { "$ref": "./stats.json" },
    "is_large": { "type": "boolean" },
    "unarmed": {
      "enum": [ "bite", "scratch", "punch", "pounce", "slash" ]
    },
    "attack_set": {
      "enum": [ "light", "quick", "slow", "intense", "varied" ]
    },
    "badly_hurt_behavior": {
      "enum": [ "fearful", "determined" ]
    },
    "variant_groups": {
      "type": "array",
      "items": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "variant": {
              "enum": [ "female", "male", "pose_a", "pose_b", "pose_c", "pose_d" ]
            },
            "weight": { "type": "integer", "minimum": 0 }
          },
          "additionalProperties": false,
          "required": [ "variant", "weight" ]
        }
      }
    }
  },
  "additionalProperties": false,
  "required": [ "default_stats", "unarmed", "attack_set" ]
}
